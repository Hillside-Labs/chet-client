# Chet

## Usage
```
NAME:
   chet - Track command times.

USAGE:
   chet [global options] command [command options] [arguments...]

COMMANDS:
   config   
   auth     
   report   
   help, h  Shows a list of commands or help for one command

GLOBAL OPTIONS:
   --addr value, -a value           (default: "http://localhost:4001") [$CHET_SERVER_ADDR]
   --client-id value, -c value       [$CHET_CLIENT_ID]
   --client-secret value, -s value   [$CHET_CLIENT_SECRET]
   --email value, -e value           [$CHET_USERNAME]
   --help, -h                       show help
```
